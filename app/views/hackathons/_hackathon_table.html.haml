%table.table.table-hover.table-striped.table-bordered{:style => 'width:100%'}
  %tbody#hackathon_list
    %tr
      %th Name
      %th Location
      %th Description
    - Hackathon.find_each do |hack|
      - @user = hack.users.find_by_username(params[:username])
      - @klass = @user.blank ? '.hide' : '.user'
      %tr.minor{:class => @klass, :'data-hack-id' => hack.id}
        %td.hackathon_name
          = hack.name
        %td.hackathon_location
          = hack.location
        %td.hackathon_desc
          = hack.desc