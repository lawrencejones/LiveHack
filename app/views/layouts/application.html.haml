!!!
%html
  %head{:'data-app-id' => Rails.application.config.app_id}
    %title LiveHack!
    = stylesheet_link_tag    "application", :media => "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
    %script
      window.font_awesome_classes = '#{File.read File.open(Rails.root.join('public','text','font-awesome-classes.txt'))}'.split(',');
  %body
    #fb-root
    = yield
